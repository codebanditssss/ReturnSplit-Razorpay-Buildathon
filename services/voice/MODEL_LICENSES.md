# Model licenses

- Whisper large-v3-turbo weights: MIT (`openai/whisper-large-v3-turbo`).
- Faster Whisper runtime: MIT (`SYSTRAN/faster-whisper`).
- Kokoro-82M v1.0 weights: Apache-2.0 (`hexgrad/Kokoro-82M`).
- Kokoro ONNX runtime wrapper: MIT (`thewh1teagle/kokoro-onnx`).
- Misaki grapheme-to-phoneme frontend: Apache-2.0 (`hexgrad/misaki`).

Review the upstream model cards before exposing this service commercially. A
small subset of Kokoro's Japanese and French training sources carry CC BY
attribution; the upstream `VOICES.md` identifies those sources.
